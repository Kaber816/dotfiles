#!/bin/bash

WALLDIR="$HOME/Pictures/Wallpapers"

WALL=$(ls "$WALLDIR" | wofi --dmenu -i -p "Wallpaper")

[ -z "$WALL" ] && exit 0

hyprctl hyprpaper unload all
hyprctl hyprpaper preload "$WALLDIR/$WALL"
hyprctl hyprpaper wallpaper ",$WALLDIR/$WALL"

